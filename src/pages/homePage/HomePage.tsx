import React from 'react';

const HomePage = () => {
    return (
        <div>
            <h4>This is main page. Cleack something in header to see some information</h4>
        </div>
    );
};

export default HomePage;